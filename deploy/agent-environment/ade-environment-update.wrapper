#!/usr/bin/env bash
set -Eeuo pipefail

source /etc/ade/agent-environment.env
exec /usr/bin/flock /run/lock/agent-environment-update.lock \
  /home/orca/.local/bin/uv run --frozen --project "$ADE_SOURCE_ROOT" \
  python -m ade.cli environment update "$@"
