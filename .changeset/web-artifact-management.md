---
"agent-dev-environment": minor
---

Integrate web publishing into the Agent environment dashboard with wallet-authorized uploads, shared CLI artifact listings, replacement confirmation, and deletion. Keep Nginx content on a separate origin and enforce upload size, path, and role restrictions.

Name the management service ADES (Agent Development Environment Service) and add an Nginx template for HTTP-to-HTTPS management access while retaining a separate HTTP artifact origin.
