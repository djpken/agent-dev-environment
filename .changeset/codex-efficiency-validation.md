---
"agent-dev-environment": patch
---

Validate the complete benchmark workspace after every turn and after independent checks, protecting read-only fixtures and rejecting unexpected files, links, deletions, and mode changes. Clean up newly registered trial trust entries in a finally block on completion, interruption, or failure, while preserving pre-existing and concurrently modified settings.
